# Pinned versions — the exact environment the results were produced with.
# Python 3.12 (hmmlearn does not yet ship wheels for Python 3.14).
numpy==2.5.1
pandas==3.0.5
scipy==1.18.0
statsmodels==0.14.6
arch==8.0.0
hmmlearn==0.3.3
scikit-learn==1.9.0
yfinance==1.5.2
pandas-datareader==0.11.1
matplotlib==3.11.1
PyYAML==6.0.3
pytest==9.1.1
pyarrow==25.0.1
jinja2==3.1.6        # pandas .to_latex() for paper tables
